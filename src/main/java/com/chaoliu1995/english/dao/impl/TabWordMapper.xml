<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.chaoliu1995.english.dao.TabWordMapper">

	<resultMap id="wordForExcelMap" type="TabWord">
		<id column="id" property="id" />
		<result column="content" property="content" />
		<result column="pronunciation" property="pronunciation" />
		<result column="definition" property="definition" />
	</resultMap>

	<resultMap id="BaseResultMap" type="TabWord">
		<id column="id" jdbcType="INTEGER" property="id" />
		<result column="uk_audio" jdbcType="VARCHAR" property="ukAudio" />
		<result column="audio_name" jdbcType="VARCHAR" property="audioName" />
		<result column="content_type" jdbcType="VARCHAR" property="contentType" />
		<result column="definition" jdbcType="VARCHAR" property="definition" />
		<result column="content_id" jdbcType="INTEGER" property="contentId" />
		<result column="has_audio" jdbcType="INTEGER" property="hasAudio" />
		<result column="content" jdbcType="VARCHAR" property="content" />
		<result column="pron" jdbcType="VARCHAR" property="pron" />
		<result column="pronunciation" jdbcType="VARCHAR" property="pronunciation" />
		<result column="us_audio" jdbcType="VARCHAR" property="usAudio" />
		<result column="memory_total" jdbcType="INTEGER" property="memoryTotal" />
	</resultMap>

	<sql id="Base_Column_List">
		id, uk_audio , audio_name , content_type, definition,
		content_id, has_audio , content, pron, pronunciation,
		us_audio,
		memory_total
	</sql>

	<insert id="insertReturnKey" parameterType="TabWord"
		useGeneratedKeys="true" keyProperty="id">
		insert into word (uk_audio,
		audio_name, content_id, has_audio,
		content, pron, pronunciation,
		us_audio)
		values
		(#{ukAudio,jdbcType=VARCHAR},
		#{audioName,jdbcType=VARCHAR},
		#{contentId,jdbcType=INTEGER},
		#{hasAudio,jdbcType=INTEGER},
		#{content,jdbcType=VARCHAR},
		#{pron,jdbcType=VARCHAR},
		#{pronunciation,jdbcType=VARCHAR},
		#{usAudio,jdbcType=VARCHAR})
	</insert>
	<select id="listTabWordForExcel" resultMap="wordForExcelMap">
		select
		id,content,pronunciation,definition from word limit
		#{start},#{limit}
	</select>
	<select id="listByColForPager" resultMap="BaseResultMap">
		select <include refid="Base_Column_List" />
		from word 
		<if test="word != null">
			<where>

			</where>
		</if>
		limit #{start},#{limit}
	</select>
	<select id="countByCol" resultType="java.lang.Integer">
		select count(id) from word
		<if test="word != null">
			<where>

			</where>
		</if>
	</select>
	<select id="getByMemoryTotalOrderEsc" resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from word order by memory_total limit 0,1;
	</select>
	<update id="memory">
		update word set memory_total = memory_total + #{num}
		where id = #{wordId}
	</update>
</mapper>